var path = require('path');

module.exports = {
  'config': path.resolve('', 'config/config.json'),
  'migrations-path': path.resolve('migrations', 'surveys'),
  'models-path': path.resolve('models', 'survey')
}
